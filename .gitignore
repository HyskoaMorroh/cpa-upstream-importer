# ── 秘密与真实配置：绝不提交 ────────────────────────────────
#
# config.yaml 含**明文上游 API Key** 与 CPA 管理密钥的 bcrypt 哈希。
# 一旦推上公开仓库，即使随后 force-push 删掉，GitHub 的 fork/缓存
# 与第三方爬虫仍可能留有副本 —— 那些 Key 必须视为已泄露并全部轮换。
#
# 这一段是本文件最要紧的部分。宁可多列几条。
config.yaml
config.yml
config.*.yaml
config.*.yml
# 例外：模板不含任何真实值，订阅 URL 与 secret 全是 ${ENV} 占位符。
# 它必须进仓库 —— 没有它，拿到镜像的人不知道该写成什么形状。
!mihomo-manager/mihomo/config.template.yaml
*.bak-*
*.scrub-bak-*
.env
.env.*
!.env.example
secrets/
auths/
*.key
*.pem
*.p12
*.pfx

# 投喂用的账号清单：每行 `url,key`，全是明文凭据
accounts.txt
accounts*.txt
upstreams.txt
*.accounts

# ── mihomo 代理：订阅 URL 与拉下来的节点清单 ────────────────
#
# mihomo-subscriptions.conf 每行是 `名称=订阅URL`，而机场订阅 URL
# **本身就是凭据** —— 拿到它就能拉全部节点、跑满流量，等同账号密码。
# 它与 .env 同级敏感，绝不能进仓库。
#
# providers/*.yaml 是按订阅拉下来的节点清单，含机场服务器地址、端口与
# 每个节点的密码。同理不公开。
#
# 进仓库的是这三个：mihomo/config.template.yaml（全 ${ENV} 占位符）、
# update-mihomo-subscriptions.sh、mihomo/healthcheck.sh，以及
# mihomo-subscriptions.conf.example（形状示例，无真实 URL）。
mihomo-manager/mihomo-subscriptions.conf
mihomo-manager/mihomo/providers/
mihomo-manager/mihomo/config.yaml
mihomo/providers/

# ── 本站排障记录：不公开 ────────────────────────────────────
#
# cpa-atlas.html 是两夜排障的全记录，里面每一条都是**本部署**的实测：
# 哪个上游站 403、哪个静默换模、哪个按出口 IP 拉黑、各站的档位与 Key 数。
# 即使域名换成占位符，这些案例组合起来仍能反推出用了哪些站、各自什么毛病。
# 本地留着当参考，不进公开仓库。
#
# 没有任何代码 import 它 —— 只有 6 处文档引用（README 2、request.py 1、
# spec 1、legacy 2），全是注释里的「依据见 xx 章」。缺它不影响运行与测试。
docs/cpa-atlas.html

# tutorial.html 含本部署的实测案例：sk-xxx key 前缀、fsdownload 路径、
# 真实域名与档位分布 —— 即使是演示性质，组合起来仍是指纹。不进公开仓库。
docs/tutorial.html

# handoff 文件是每日交接记录，含具体站名、Key 数量、排障思路。不进仓库。
docs/*-handoff.md

# ── 工具生成物 ──────────────────────────────────────────────
#
# graphify-out/ 是代码图谱（2.1 MB），每次 `graphify .` 重新生成。
# 它把源码里的字符串抄进 graph.json / graph.html，所以**会连带泄露**
# scrub 之前的域名 —— 实测 graph.json 里就留着一个真实站名。
# 生成物不进仓库，需要图谱的人自己跑一次。
graphify-out/

# ── 运行期产物 ──────────────────────────────────────────────
logs/
*.log
backups/
tmp/
*.tmp

# ── Python ──────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ── 编辑器 / 系统 ───────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# 站点代号对照 —— 含真实上游站名，与 cpa-atlas.html 同理
docs/SITE-CODENAMES.md

# ── 本部署的排查与整改记录：不公开 ──────────────────────────
#
# 与 cpa-atlas.html 同一条理由，且更敏感：这两份写的是**本部署**的
# 具体状态 —— VPS 上哪些管理面公网直达、nginx 缺哪几条防护、密钥放在
# 哪个同步目录、各上游站分别什么毛病、档位分裂在哪几组。
# 即使域名全部换成代号，这些细节组合起来仍是一份现成的攻击面清单。
#
# 它们是给运维本人看的排障记录，不是给公开仓库的文档。
# 通用的机制说明已经写进 README 与 docs/tutorial.html（那两份不含本部署细节）。
docs/2026-09-10-方案-agi任务.md
docs/2026-09-11-部署侧整改方案.md

# ── mihomo 代理私密配置（2026-09-13）──────────────────────────────
# config.yaml：从模板生成，含订阅 URL（token）与 API secret
# providers/：mihomo 自动拉取的订阅缓存，含节点地址与密码
# mihomo-subscriptions.conf：订阅清单，含 token
mihomo-manager/mihomo/config.yaml
mihomo-manager/mihomo/providers/
mihomo-manager/mihomo-subscriptions.conf

# .env：部署环境变量，含 MIHOMO_SECRET、MIHOMO_SUB_* 订阅 URL
.env
